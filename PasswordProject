//This program will help to create a validated password for the user
#include <iostream>
#include <string>
#include <cstring>
#include <cctype>

using namespace std;

string passwordOne;
string passwordTwo;
int i;

//creating function
void func_password (string& passwordOne);
void func_passwordMatch (string& passwrodTwo, string&passwordOne);

int main() {
     char loop;
     
     
    cout<< "This program will help you to create and validate a password"<< endl;
    
    do{
        func_password (passwordOne);
        func_passwordMatch (passwordTwo, passwordOne);
        
        cout<< "Do you want to restart?";
        cin>>loop;
          
    } while (loop == 'y' || loop == 'Y');
    
    
    }
    
void func_password(string& passwordOne) {
    int passwordLength;
    char validation = 'y';
    bool upper = false;
    bool lower = false;
    bool number = false;

    
    cout<< "Please enter a password with the following requirements: \n     1. Password must be at minimum 8 characters\n     2. Password must be at maximum 20 characters\n     3. Password must contain atleast 1 number\n     4. Password must contain at least one capital letter\n";
    do{
    do{
cout<< "Please enter your password.\n";
    cin>>passwordOne;
    
    passwordLength = passwordOne.length();
    
    cout<< "your password is " << passwordOne.length() <<" characters long, ";
    
    if (passwordLength< 8 || passwordLength>20) {
        cout<< "please try again your password length does not meet the requirements \n\n";
        validation = 'n';
        }
        
    else if (passwordLength >= 8 && passwordLength<20) {
        cout<< "your password meets the length requirements\n\n";
        validation = 'y';
    }
    
    
    }while (validation == 'n');



for (char c : passwordOne) {
    if(isupper(c)) {
        upper = true;
        
    }
        
    if(islower(c)){
        lower = true;
    
    }
    if (isdigit(c)){
        number = true;
    
    }
}
    
    
    if (upper == false) {
        cout<< "Your password does not contain an uppercase letter\n";
    }
    
    if (lower == false) {
        cout<< "Your password does not contain a lowercase letter\n";   
    }
    
    if (number == false){
        cout<< "Your password does not contain a number\n";
    }
        
    }while (upper == false || lower == false || number == false);
    if (upper == true && lower == true && number == true){
        cout<< "all requirements reached\n"; }
                    
}

void func_passwordMatch(string& passwordTwo, string& passwordOne) {
    
    i = 1;
    do{
    
    cout<< "attempts: " << i << "/3\n";
    cout<< "please re-enter your password.\n";
    cin>> passwordTwo;
    
    i++;
    
    if (passwordTwo == passwordOne){
        cout<<"\nPassword accepted!\n\n";}

    }while (passwordOne != passwordTwo && i < 4);
    if (i == 4 && passwordOne != passwordTwo) {
        cout<< "Your password did not match, please restart the program or try again at a later time. \n\n";
    }
}
